# Contributing

Use Node 22 and pnpm 11. Preserve strict TypeScript, validate boundaries with Zod, add tests for behavior changes, run `pnpm verify`, and update `BUILD_LOG.md` and `KNOWN_LIMITATIONS.md`. Never commit `.env` files.
