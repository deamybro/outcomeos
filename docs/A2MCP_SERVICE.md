# A2MCP services

The Contract Compiler and Proof Check endpoints are free during the hackathon. They use stable JSON envelopes with correlation IDs. The compiler labels user/template/AI sources; proof checks never turn verifier errors into passes.
