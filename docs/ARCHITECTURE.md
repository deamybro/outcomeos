# Architecture

Browser → Next.js Route Handler → Zod validation → contract compiler or verification engine → deterministic verifier → scoring → canonical SHA-256 receipt. Gemini is an optional bounded suggestion provider. Supabase is the durable store and enforces owner RLS. GitHub inspection uses Octokit and never executes repository code.
