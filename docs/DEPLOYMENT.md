# Deployment

Apply the Supabase migration, configure Vercel environment variables, deploy from the locked repository, then verify HTTPS, `/api/health`, both A2MCP endpoints, a public receipt, CSP headers and secret absence. Production smoke tests are mandatory and cannot be completed without a real deployment URL.
