# Environment variables

See `.env.example`. `NEXT_PUBLIC_*` values are intentionally public. `SUPABASE_SERVICE_ROLE_KEY`, `GEMINI_API_KEY`, and `GITHUB_TOKEN` are server-only. `ADMIN_EMAILS` is an initial allowlist and must be combined with authenticated server verification.
