# Testing

`pnpm lint`, `pnpm typecheck`, `pnpm test:unit`, `pnpm test:integration`, `pnpm test:e2e`, and `pnpm build`. Tests use controlled input or mocked external services. Never point automated SSRF tests at private production resources.
