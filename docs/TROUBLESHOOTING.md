# Troubleshooting

Missing Gemini key activates deterministic fallback. GitHub without a token has lower rate limits. Unsafe URL errors mean the destination resolved to a prohibited address. Use the response request ID to correlate sanitized server logs.
