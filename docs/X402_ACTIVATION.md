# x402 activation

The adapter is disabled by default. Activation requires legal/product approval, audited receiver/network/asset/amount configuration, replay protection, verified payment responses and an end-to-end staging test. Set `PAYMENT_MODE=free` and `ENABLE_X402=false` until that gate passes.
